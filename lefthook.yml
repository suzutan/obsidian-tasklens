pre-commit:
  parallel: true
  commands:
    biome:
      run: npx biome check src/
    oxlint:
      run: npx oxlint src/
    typecheck:
      run: npx tsc --noEmit --skipLibCheck
    test:
      run: npm test
